(include_subdirs qualified)

(coq.theory
 (name PTree)
 (package coq-ptree)
 (modules
  (:standard
   ;; These experimental clients require a universe-polymorphic PTree to
   ;; host MathComp measurable kernels.  The MathComp measure and AST layers
   ;; themselves remain part of the installed theory.
   \ Examples.VonNeumannMathComp
   Examples.RealBernoulliMathCompFactory
   ;; This older client derives its nested loop through pstructural and the
   ;; syntax-directed canonical-nested adequacy theorem.  Keep the source as
   ;; migration material, but do not present it as part of the independent
   ;; operational model; OperationalRationalBernoulli is the maintained
   ;; primitive-hitting p-to-q example.
   Examples.OperationalBernoulliFactory))
 (theories
   ExtLib
   ITree
   Paco
   Coinduction
   RelationAlgebra
   ;; ProbAx

   ;; math comp
   mathcomp
   HB
   mathcomp.analysis
   elpi
   elpi_elpi
 )
 (flags -w -notation-incompatible-prefix -w -notation-overridden))
